spring:
  datasource:
    url: jdbc:mysql://localhost:3306/papertrail_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update

app:
  jwt:
    secret: local-dev-secret-at-least-32-characters-long
    expiration-ms: 86400000
  otp:
    expiration-minutes: 10

cors:
  allowed-origins: http://localhost:8081,http://localhost:19006,exp://127.0.0.1:19000
